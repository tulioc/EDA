/*#include <stdio.h>
#include <stdlib.h>

Uma forma melhor de organizar as listas é separar as estruturas como
Nó = o tipo de dados que você está armazenando
Lista = sua lista de fato.

typedef struct _no
{

}No;

typedef struct _lista
{
  struct _no * no;
  struct _lista * proximo;
}Lista;
*/
